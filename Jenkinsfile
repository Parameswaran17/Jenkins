pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build an app'
            }
        }
        stage('Test') {
            steps {
                echo 'Test an app'
            }
        }
       stage('Deploy') {
            steps {
                echo 'Deploy an app'
            } 
        }
    }
}    
